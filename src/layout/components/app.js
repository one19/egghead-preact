import { h } from 'preact';
import Layout from './layout';

// React = preact;
// React.createElement = preact.h;
// React.PropTypes = { func:{} };

export default () =>
  <div className="app">
    <Layout />
  </div>;
